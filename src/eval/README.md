# eval

Runs the pack's evidence-eval fixtures and replays the wake log with no model.

Self-declared success is not enough: a fixture the agent declared for itself is
listed, marked not independent, and does not count.
