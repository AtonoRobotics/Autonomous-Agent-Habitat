# data

PostgreSQL is the only business truth. Domain kinds are data, not schema.

A missing database URL fails closed rather than falling back to an in-process
ledger. Migrations live in `/migrations`.
