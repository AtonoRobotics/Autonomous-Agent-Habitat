# agents

Agent records instantiated 1:1 from the loaded pack's `roles[]` — name, persona,
skills, specialties, and which role is flagged Orchestrator. No product-constant
count, no ad-hoc spawn: the org chart is the pack.

Also holds inter-agent mail. Mail is untrusted content and never confers authority.
