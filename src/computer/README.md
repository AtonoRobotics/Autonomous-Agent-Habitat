# computer

One real, persistent Linux computer per tenant — namespaces by default, container
optional — with a shared disk, covered host secrets, and no network unless an
allowlist is bound.

Path containment is checked after resolution; lexical checks are not sufficient.
Field users configure no part of the machine.
