# connectors

Architect-bound world handles — base URL plus credentials, stored on the tenant's
disk. Models receive handles, never secrets.

Typed refusals belong here: unbound, credentials missing, unreachable, credentials
rejected, send incomplete, rejected.
