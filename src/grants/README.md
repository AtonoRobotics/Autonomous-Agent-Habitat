# grants

Bounded owner authorization per action class: state, channels, purposes, subject
scope, rate per hour, expiry.

A grant reaches `authorized` only with independent outcome evidence ids, eval ids,
and a plain-language notice — otherwise the write is refused as a surprise
graduation. Graduation never strips the policy gateway.
