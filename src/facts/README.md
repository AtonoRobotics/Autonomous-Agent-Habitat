# facts

Assertions with provenance: subject, predicate, typed value, classification
(verified, reported, observed, inferred, unknown, contradicted, expired),
confidence, source.

A model can never write `verified`. Memory never becomes fact.
